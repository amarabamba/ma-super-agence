framework:
    secret: '%env(APP_SECRET)%'
    csrf_protection: true

    # Enables session support.
    session:
        handler_id: null
        cookie_samesite: lax

    php_errors:
        log: true